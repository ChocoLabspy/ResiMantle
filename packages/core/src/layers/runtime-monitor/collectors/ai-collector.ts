// This module is intentionally left as a placeholder.
// AI call interception is handled by the AiProxy in the AI Defense layer.
// This collector is reserved for framework-specific hooks (e.g., LangChain).
export class AiCollector {
  collect(): void {
    // Reserved for AI framework interception
  }
}
