// This module is intentionally left as a placeholder.
// Database interception requires driver-specific hooks
// (pg, mysql2, mongodb) and will be implemented per-driver.
export class DbCollector {
  collect(): void {
    // Reserved for database driver interception
  }
}
